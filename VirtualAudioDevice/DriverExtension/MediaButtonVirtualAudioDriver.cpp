/*
 Reserved scaffold for a DriverKit-based implementation if the final shipping
 virtual microphone needs a Driver Extension instead of, or in addition to, the
 user-space AudioServerPlugIn path.

 The current repo does not have a buildable Driver Extension target because this
 machine only has Command Line Tools. Full implementation belongs in an Xcode
 project with the required DriverKit entitlements and signing setup.
 */
